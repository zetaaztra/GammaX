name: Weekly Full Pipeline & Training

on:
  schedule:
    - cron: "30 20 * * 0"  # Sunday 2:00 AM IST (8:30 PM UTC Saturday)
  workflow_dispatch:

jobs:
  cleanup-old-data:
    name: Clean up old cache and data
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Clean old workflow runs
        uses: actions/github-script@v7
        with:
          script: |
            const days_to_keep = 30;
            const cutoff = new Date(Date.now() - days_to_keep * 24 * 60 * 60 * 1000);
            
            const runs = await github.rest.actions.listWorkflowRunsForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              per_page: 100
            });
            
            for (const run of runs.data.workflow_runs) {
              if (new Date(run.created_at) < cutoff) {
                console.log(`Deleting run ${run.id} from ${run.created_at}`);
                await github.rest.actions.deleteWorkflowRun({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  run_id: run.id
                });
              }
            }
      
      - name: Clear GitHub Actions cache
        run: |
          gh extension install actions/gh-actions-cache
          
          echo "Fetching list of cache keys"
          cacheKeys=$(gh actions-cache list -R $REPO -B $BRANCH | cut -f 1)
          
          echo "Deleting caches..."
          for cacheKey in $cacheKeys
          do
            gh actions-cache delete $cacheKey -R $REPO -B $BRANCH --confirm
          done
          
          echo "Done clearing cache"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          BRANCH: ${{ github.ref }}

  full-pipeline:
    name: Generate all stock data & train models
    needs: cleanup-old-data
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Fetch tickers list
        run: python scripts/fetch_tickers.py
      
      # STEP 1: Full fetch of all 503 tickers (generates CSVs)
      - name: Fetch all 503 stocks data (generates CSVs)
        run: |
          python scripts/tradyxa_pipeline.py \
            --mode batch_run \
            --tickers-file scripts/nifty500.txt \
            --max-workers 4 \
            --use-yf
        timeout-minutes: 45
      
      # STEP 2: Train ML models on the fetched data
      - name: Train regime classification model
        run: python scripts/train_regime_classifier.py
      
      - name: Train slippage forecasting models
        run: python scripts/train_slippage_quantile.py
      
      # STEP 3: Apply predictions to all JSONs
      - name: Apply ML predictions to all stocks
        run: python scripts/apply_models.py
      
      # STEP 4: Fetch live spot prices
      - name: Fetch live spot prices
        run: python scripts/fetch_spot_prices.py
      
      # STEP 5: Single commit and push
      - name: Commit and push all changes
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # Add ALL generated files (CSVs, JSONs, models)
          git add -A
          
          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📊 Weekly full pipeline: data + models [skip ci]"
            
            # Pull latest (in case of concurrent changes) and push
            git pull origin main --no-rebase --allow-unrelated-histories || true
            git push origin main
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Summary
        run: |
          echo "✅ Weekly pipeline completed successfully!"
          echo "📊 Generated data for 503 stocks"
          echo "💾 CSVs stored in public/data/raw/"
          echo "🤖 Trained and applied ML models"
          echo "💰 Fetched live spot prices"
          echo "🧹 Cleaned up old caches"
